# Minimal redis.conf shipped alongside basic.hcl as a placeholder
# `file()` source. Replace with your real production config.

bind 0.0.0.0
port 6379
appendonly yes
dir /data
maxmemory 256mb
maxmemory-policy allkeys-lru
